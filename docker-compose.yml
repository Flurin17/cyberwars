version: '3.8'

services:
  cyberwars:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./uploads:/var/www/html/uploads
      - ./data:/var/www/html/data
    environment:
      - PHP_CLI_SERVER_WORKERS=4
